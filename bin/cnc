#!/bin/bash

case "$1" in
    start)
        systemctl --user start cnc
        ;;
    stop)
        systemctl --user stop cnc
        ;;
    *)
        echo "Usage: cnc {start|stop}"
        exit 1
        ;;
esac
